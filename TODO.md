- [ ] Locate existing middleware and auth implementation
- [ ] Fix middleware to enforce ADMIN-only access to /admin routes
- [ ] Ensure middleware relies on JWT role (based on response A)
- [ ] Tighten middleware matcher to only run on /admin and skip static assets
- [ ] Run frontend lint/build/tests (if available) to confirm no TS errors

